import {FC} from 'react';
import {observer} from 'mobx-react-lite';

import styles from './index.module.scss';

export const ModalSignUp: FC = observer(() => {
  return (
    <div>
      Sign Up
    </div>
  );
});
